import React, { FC } from "react";
import { plus_Jakarta_Sans } from "../utils/fonts";

const Header: FC = () => {
  return (
    <header
      className={`bg-black border-b border-gray-700 ${plus_Jakarta_Sans.className}`}
    >
      test
    </header>
  );
};

export default Header;
